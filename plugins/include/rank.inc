#if defined _rank_included
 #endinput
#endif
#define _rank_included
native SetSpeed(client, speed);
native SetDeflect(client, deflect);
native SetSumDeflect(client);
native GetSpeed(client);
native GetDeflect(client);